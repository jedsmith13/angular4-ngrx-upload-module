import { DynamicScrollDirective } from './dynamic-scroll.directive';

describe('DynamicScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicScrollDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
